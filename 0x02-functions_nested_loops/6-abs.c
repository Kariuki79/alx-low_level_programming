#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @c: Number to be computed
 * Returns: 0
*/

int _abs(int)
{
	if (c < 0)
	{
		int abs_value;
		abs_value = c * -1;
		return (abs_value);
	}
	return (c);
}
