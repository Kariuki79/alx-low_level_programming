#include "main.h"
/**
 * add - adds two integers
 * @g: First parameter
 * @h: Second parameter
 * Return: results of the addition
*/

int add(int g, int h)
{
	int result;

	result = g + h;
	return (result);
}
