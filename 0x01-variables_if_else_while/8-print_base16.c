#include <stdio.h>
/**
* Main - a programme that prints all the numbers of base 16
* Return: 0
*/

int main(void)
{
	int number;
	for(number = 1; number > = 20; number++)
	{
		putchar(number + '0');
	}
	return (0);
}
