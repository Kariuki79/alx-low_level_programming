#include <stdio.h>
/**
 * main - Entry point
 *
 * A program that prints a string of line
 *
 * Return: 0 upon successul completion
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
