#include "main.h"
/**
 * puts2 -prints every other character of a string
 * @str
 */

void puts2(char *str)
{
	int m;

	for (a = 0; s[m] != '\0'; a++)
	{
		if (a % 2 == 0)
		{
			_putchar(s[a]);
		}
	}
}
