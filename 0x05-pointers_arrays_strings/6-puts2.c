#include "main.h"
/** put2 -prints every other charater of a string
 * @str: stores character to be printed
 */
void puts2(char *str);
{
	int m;

	for (m = 0; s[m] != '\0'; m++
			{
			if (m %2 == 0;
					{
					putchar(s[m]);
					}
					}
					_putchar('\n');
					}

